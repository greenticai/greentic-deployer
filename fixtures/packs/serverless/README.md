# Serverless Container Deployment Pack Fixture

This fixture represents the `greentic.deploy.serverless` deployment pack for `PR-07`.

It models a normalized serverless container target with:

- userspace SquashFS only
- writable `/tmp`
- external Redis required
- private or disabled admin API

