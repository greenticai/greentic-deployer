{
  "type": "object",
  "required": [
    "kind",
    "capability",
    "provider",
    "strategy",
    "descriptor_path",
    "required_secrets",
    "health_endpoints"
  ],
  "properties": {
    "kind": { "const": "generate" },
    "capability": { "const": "generate" },
    "provider": { "const": "generic" },
    "strategy": { "const": "serverless-container" },
    "descriptor_path": { "const": "assets/examples/deployment-descriptor.json" },
    "required_secrets": {
      "type": "array",
      "items": { "type": "string" }
    },
    "health_endpoints": {
      "type": "array",
      "items": { "type": "string" },
      "contains": { "const": "/healthz" }
    }
  }
}

