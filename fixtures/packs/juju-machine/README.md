# Juju Machine Deployment Pack Fixture

This fixture represents the `greentic.deploy.juju-machine` deployment pack for `PR-06`.

It models a machine charm that installs and manages the Snap-based operator.

